import { Given, When, Then } from '@cucumber/cucumber';
import { expect } from '@playwright/test';

Given('I am on the checkout page', async function () {
    // page.goto(checkoutUrl)
});
When('I enter valid card details', async function () {
    // Fill card details
});
